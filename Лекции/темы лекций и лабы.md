
# Лекции

## **Лекция 1: Инструменты и основы разработки**

- **Темы**:
  - VS Code (настройка Java, расширения)
  - Maven/Gradle (сравнение, структура проекта).
  - XML/JSON/YAML (форматы данных).
  -  Фреймворк vs библиотека (на примере Spring и Apache Commons).
  - Git


### Лаба 1.1: Создать Maven-проект в VS Code, добавить Spring Boot, Lombok, Jackson. Настроить расширения.
### Лаба 1.2: Реализовать сериализацию/десериализацию Product в JSON и YAML через ObjectMapper. Сравнить форматы
---

## **Лекция 2: Архитектура и HTTP**

- **Темы**:
  - Клиент-серверная архитектура, методы HTTP (GET/POST/PUT/DELETE), статус-коды.
  - 
    - TCP/IP (handshake, retransmission, bandwidth-delay product — _как это влияет на скорость запросов_).
    - Flow Control и Head of Line Blocking (_почему HTTP/2 решает проблему_).
- **Пример**: Анализ запросов через Chrome DevTools (Network tab).


### Лаба 2.1: Создать ProductController с GET /products, POST /products. Проверить статусы 200, 201, 404.
### Лаба 2.2: Проанализировать запросы в Chrome DevTools: время, заголовки, размер, причины задержек (на примере handshake, flow control)
---

## **Лекция 3: Spring Core — DI/IoC и Bean**

- **Темы**:
  - DI/IoC (на примере `@Autowired`), жизненный цикл Bean.
  - 
    - Понятие контейнера (Container), почему IoC упрощает тестирование.
    - Скоупы (`@Scope`) — _связь с жизненным циклом объектов в многопоточной среде_.
- **Пример**: Простой сервис с внедрением зависимостей (без Spring Boot — через `AnnotationConfigApplicationContext`).


### Лаба 3.1: Реализовать ProductService и внедрить в ProductController через @Autowired.
### Лаба 3.2: Создать ShoppingCart с @Scope("prototype"), проверить, что каждый запрос создаёт новый экземпляр
---

## **Лекция 4: Веб-серверы и многопоточность**

- **Темы**:
  - Web-сервер (Tomcat/Jetty), обработка запросов.
  - 
    - Threading model (threads per connection vs thread pool), `ExecutorService`.
    - Race condition (на примере счетчика запросов), синхронизация через `synchronized`.
    - Amdahl’s law (_почему параллельность не решает все проблемы_).
- **Пример**: Сервлет с инкрементом счетчика (демонстрация гонки).


### Лаба 4.1: Создать сервлет с AtomicInteger requestCounter, показать гонку при 100 параллельных запросах.
### Лаба 4.2: Исправить проблему через synchronized и сравнить с @Async + TaskExecutor
---

## **Лекция 5: Spring MVC и параметры запросов**

- **Темы**:
  - `@RequestParam`, `@PathVariable`, модель данных.
  - 
    - GET-параметры (как кодируются в URL), безопасность (почему пароли не в GET).
    - Flow Control в HTTP (как браузер управляет загрузкой ресурсов).
- **Пример**: Поисковая форма с обработкой `?query=java`.


### Лаба 5.1: Реализовать поиск: GET /products?query=phone, выводить результат через Thymeleaf.
### Лаба 5.2: Добавить GET /products/{id}, отображать детали товара, использовать Model
---

## **Лекция 6: CRUD и архитектура**

- **Темы**:
  - CRUD-операции, HTML-формы, `@Valid`.
  - 
    - Паттерн DAO (Data Access Object), почему он изолирует логику БД.
    - REST (почему PUT vs POST, идемпотентность).
- **Пример**: Форма создания пользователя с валидацией.


### Лаба 6.1: Реализовать форму создания товара (name, price, category) через Thymeleaf.
### Лаба 6.2: Выделить ProductDAO и ProductService, реализовать паттерн DAO без Spring Data
---

## **Лекция 7: Spring Data JPA и транзакции**

- **Темы**:
  - `JpaRepository`, `@Entity`, `@Repository`.
  - 
    - ACID (атомарность, изоляция), почему транзакции нужны в DAO.
    - ORM vs JDBC (плюсы/минусы).
- **Пример**: Сохранение сущности `User` с валидацией.


### Лаба 7.1: Подключить PostgreSQL, создать Product с @Entity, ProductRepository extends JpaRepository.
### Лаба 7.2: Реализовать OrderService с @Transactional, проверить откат при ошибке
---

## **Лекция 8: Запросы и оптимизация**

- **Темы**:
  - Query Methods, `@Query`, N+1 problem.
  - 
    - Кэширование (уровни: БД, приложение, браузер), `@Cacheable`.
    - Пагинация (LIMIT/OFFSET vs cursor-based).
- **Пример**: Поиск товаров с пагинацией через `Pageable`.


### Лаба 8.1: Добавить findByPriceGreaterThan и @Query("SELECT p FROM Product p WHERE p.category = :cat").
### Лаба 8.2: Реализовать пагинацию (Pageable) и @Cacheable для списка товаров
---

## **Лекция 9: Spring Boot и файлы**

- **Темы**:
  - Статика (`/static`), загрузка файлов.
  - 
    - MIME-типы (почему изображения имеют разные расширения), multipart/form-data.
    - Безопасность (проверка MIME-типов на сервере).
- **Пример**: Загрузка аватарки с валидацией формата.


### Лаба 9.1: Загрузить изображение товара в static/images, отобразить через Thymeleaf.
### Лаба 9.2: Проверить MIME-тип файла на сервере, отклонить .exe, .sh
---

## **Лекция 10: Spring Security — аутентификация**

- **Темы**:
  - `UserDetailsService`, сессии, cookies.
  - 
    - Хэширование (bcrypt vs SHA-256), CSRF (почему токены нужны).
    - Сессии vs stateless (JWT).
- **Пример**: Защита `/admin` с кастомной формой логина.


### Лаба 10.1: Защитить /admin/**, реализовать UserDetailsService с InMemoryUserDetailsManager.
### Лаба 10.2: Настроить кастомную форму логина, хранить пароли через BCryptPasswordEncoder.

---

## **Лекция 11: JWT и асинхронность**

- **Темы**:
  - Генерация JWT, хранение в cookies (`HttpOnly`).
  - 
    - Inter-process communication (синхронные vs асинхронные вызовы).
    - Publish-subscribe (на примере Guava EventBus).
- **Пример**: Отправка уведомления через `@Async` после генерации токена.


### Лаба 11.1: Сгенерировать JWT при логине, вернуть в ответе.
### Лаба 11.2: Отправить email через @Async после регистрации (с логом вместо реальной отправки)
---

## **Лекция 12: Kafka и отказоустойчивость**

- **Темы**:
  - `@KafkaListener`, обработка сообщений.
  - 
    - Fault tolerance (репликация, партиционирование), partial failures.
    - Idempotency (почему важно для финансовых операций).
- **Пример**: Логгирование заказов в Kafka при создании.


### Лаба 12.1: Отправлять событие order.created в Kafka при создании заказа.
### Лаба 12.2: Слушать топик в другом сервисе, логировать сообщение, добавить retry
---

## **Лекция 13: Тестирование и логгирование**

- **Темы**:
  - JUnit, Mockito, `@MockBean`, SLF4J.
  - 
    - Виды тестов (unit, integration), когда использовать моки.
    - Уровни логгирования (DEBUG vs ERROR).
- **Пример**: Тест сервиса с мокированием репозитория.


### Лаба 13.1: Написать юнит-тест ProductService с @MockBean репозитория.
### Лаба 13.2: Добавить логгирование в OrderService через @Slf4j, выводить INFO при создании
---

## **Лекция 14: Docker и CI/CD**

- **Темы**:
  - `Dockerfile`, Docker Compose.
  - 
    - Изоляция процессов (namespaces, cgroups), преимущества контейнеров.
    - CI/CD pipeline (build → test → deploy).
- **Пример**: Запуск Spring Boot + PostgreSQL в Docker.


### Лаба 14.1: Написать Dockerfile, упаковать приложение, запустить.
### Лаба 14.2: Создать docker-compose.yml с Spring Boot + PostgreSQL, подключиться
---

## **Лекция 15: Jenkins и мониторинг**

- **Темы**:
  - Автодеплой, Prometheus, Grafana.
  - 
    - Observability (логи, метрики, трейсы), SLO/SLA.
    - Баланс между cost и надежностью.
- **Пример**: Настройка алерта при падении uptime.


### Лаба 15.1: Настроить Jenkins: сборка → тесты → деплой контейнера.
### Лаба 15.2: Подключить Actuator, настроить Prometheus, создать дашборд в Grafana.

---

## **Лекция 16: Проектирование ПО**

- **Темы**:
  - SOLID, паттерны (Strategy, Singleton).
  - 
    - Технический долг, когда рефакторить.
    - Trade-offs (нормализация БД vs производительность).
- **Пример**: Рефакторинг кода с антипаттерном «Божественный объект».


### Лаба 16.1: Рефакторить "Божественный объект" — разбить OrderService на OrderCreation, PaymentService.
### Лаба 16.2: Применить паттерн Strategy для выбора способа оплаты (Card, PayPal)
---

## **Лекция 17: Lombok и рефлексия**

- **Темы**:
  - `@Data`, `@Builder`, аннотации.
  - 
    - Рефлексия в Java (как Spring обрабатывает аннотации).
    - Когда использовать Lombok (и когда не использовать).
- **Пример**: Сравнение POJO с геттерами/сеттерами и через Lombok.

### Лаба 17.1: Заменить POJO с геттерами/сеттерами на @Data, проверить байт-код через javap.
### Лаба 17.2: Создать утилиту, которая через рефлексию выводит все поля Product.

---

## **Лекция 18: Поиск и фильтрация**

- **Темы**:
  - Динамические запросы, `Specification`.
  - 
    - Индексы в БД (как ускорить поиск), full-text search.
    - Фильтрация на клиенте vs сервере.
- **Пример**: Фильтр товаров по цене и категории.

### Лаба 18.1: Реализовать динамический поиск по цене, категории, названию через Specification.
Лабa 18.2: Добавить пагинацию и сортировку в запрос.

---

## **Лекция 19: Транзакции и блокировки**

- **Темы**:
  - `@Transactional`, изоляция.
  - 
    - Deadlock (как возникает), optimistic vs pessimistic locking.
    - Thread blocking (на примере блокирующих IO-операций).
- **Пример**: Перевод денег между счетами с транзакцией.

### Лаба 19.1: Реализовать перевод между счетами с @Transactional, проверить откат.
### Лаба 19.2: Смоделировать deadlock (2 потока, разный порядок блокировок), исправить через таймаут.

---

## **Лекция 20: Non-blocking IO**

- **Темы**:
  - Spring WebFlux, `Mono`, `Flux`.
  - 
    - Reactor pattern, event loop.
    - Когда использовать WebFlux (и когда не нужно).
- **Пример**: Обработка 10K запросов через WebFlux vs MVC.

### Лаба 20.1: Переписать ProductController на WebFlux (Mono<Product>).
### Лаба 20.2: Сравнить производительность MVC и WebFlux под нагрузкой (JMeter, 1000 запросов).

---

## **Лекция 21: Git и командная работа**

- **Темы**:
  - GitFlow, PR, конфликты.
  - 
    - Distributed VCS (почему Git лучше SVN), семантическое версионирование.
- **Пример**: Разрешение конфликта в мерж-реквесте.

### Лаба 21.1: Создать ветку feature/reviews, добавить Review, сделать PR.
### Лаба 21.2: Создать конфликт в pom.xml, разрешить вручную, выполнить мерж.

---

## **Лекция 22: Финальный проект — архитектура**

- **Темы**:
  - Выбор стека (Spring Boot + React), разделение на модули.
  - 
    - Микросервисы vs монолит, bounded contexts (DDD).
    - Тестирование интеграционных точек.
- **Пример**: Демо-архитектуры интернет-магазина с выделением сервисов.

### Лаба 22.1: Создать UML-диаграмму модулей: product-service, user-service, order-service.
### Лаба 22.2: Выделить NotificationService в отдельный модуль (подпроект Maven).

---

## **Лекция 23: Финальный проект — безопасность и масштаб**

- **Темы**:
  - Rate limiting, шифрование данных.
  - 
    - Defense in depth, принцип минимальных привилегий.
    - Горизонтальное vs вертикальное масштабирование.
- **Пример**: Настройка rate limit через Spring Cloud Gateway.

### Лаба 23.1: Настроить rate limiting через Spring Cloud Gateway (10 запросов/сек на IP).
### Лаба 23.2: Зашифровать email в БД через AES, расшифровывать при чтении.

---

## **Лекция 24: Обзор и best practices**

- **Темы**:
  - Когда использовать Spring, а когда нет.
  - 
    - Технический стэк как компромисс (скорость vs надежность).
    - Как учиться после курса (паттерны, RFC, open-source).
- **Пример**: Сравнение Spring Boot с Micronaut/Quarkus.

### Лаба 24.1: Сравнить Spring Boot и Micronaut: запустить простой сервис на обоих, сравнить время старта.
### Лаба 24.2: Самостоятельно реализовать модуль "Аналитика": загрузить 50K товаров из CSV, построить топ-10 по продажам.

---
