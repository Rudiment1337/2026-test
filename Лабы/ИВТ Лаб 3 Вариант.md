# ✅ Проект №3: Система обработки аварийных инцидентов
## 🎯 Цель
- Быстрое реагирование на ДТП, резкие торможения, падения пассажиров.

## 📌 Описание
- Система получает сигналы от датчиков (акселерометр, камера, кнопка экстренного вызова), формирует инцидент, уведомляет диспетчера, позволяет прикрепить фото/видео, формирует акт.

## 🧱 Модели
```
public class Alert {
    private Long id;
    private Bus bus;
    private EventType type;  
    private LocalDateTime timestamp;
    private String location;
    private String description;
    private StatusType status;
    private List<String> photoUrls;
    private User assignedTo;
}

public class Bus {
    private Long id;
    private String model;
}

public enum EventType {
    "accident", "hard_braking", "button"
}
public enum StatusType {
    "new", "in_progress", "resolved"
}
```

## 🌐 API
- POST /api/incidents – создание инцидента
- GET /api/incidents?status=new – новые инциденты
- PUT /api/incidents/{id}/assign – назначить ответственного
- PUT /api/incidents/{id}/status_change - сменить статус
- POST /api/incidents/{id}/photos – загрузка фото


## 🚀 Доп
- Автоматическое формирование pdf отчета о закрытом Alert
- Загрузка фото через форму Thymeleaf